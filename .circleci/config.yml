version: 2.1

executors:
  docker-postgres:
    docker:
      - image: cimg/base:current
      - image: cimg/postgres:11.17
      - image: cimg/postgres:11.17
        name: bigbird
    resource_class: large

jobs:
  test:
    executor: docker-postgres
    steps:
      - checkout

workflows:
  main:
    jobs:
      - test
